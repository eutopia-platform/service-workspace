type Workspace {
  name: String!
  id: ID!
}

type Query {
  hello: String
  workspace(name: String!): Workspace
}

type Mutation {
  createWorkspace(name: String!): Workspace
}

schema {
  query: Query
  mutation: Mutation
}

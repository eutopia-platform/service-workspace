type Workspace {
  name: String!
}

type Query {
  hello: String
  workspace(name: String!): Workspace
  workspaces: [Workspace!]!
}

type Mutation {
  createWorkspace(name: String!): Workspace
}

schema {
  query: Query
  mutation: Mutation
}
